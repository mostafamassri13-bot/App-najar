workflows:
  android-workflow:
    name: Android Build Workflow
    max_build_duration: 60
    instance_type: mac_mini_m1
    scripts:
      - name: Inspect ZIP Contents
        script: |
          unzip مساعد-النجار-الذكي.zip
          echo "=== طباعة كافة الملفات والمجلدات المستخرجة ==="
          find . -maxdepth 3
          echo "============================================="
